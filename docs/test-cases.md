# 手动测试用例

## TC01 管理员登录

功能：身份验证  
输入：用户名 `admin`，密码 `admin123`  
预期输出：进入管理员菜单  
实际输出：输入 `admin/admin123` 后显示“登录成功，欢迎 系统管理员”并进入管理员菜单。  
结果：通过

## TC02 玩家登录

功能：身份验证  
输入：用户名 `P001`，密码 `123456`  
预期输出：进入玩家菜单  
实际输出：输入 `P001/123456` 后可进入玩家菜单。  
结果：通过

## TC03 错误登录

功能：身份验证  
输入：用户名 `admin`，密码 `wrong`  
预期输出：提示通用登录失败，不暴露账号角色
实际输出：密码错误时显示“登录失败，请检查用户名和密码”，不会显示“管理员账号登录失败”。
结果：通过

## TC04 按 ID 查询玩家

功能：玩家查询  
输入：`P001`  
预期输出：显示玩家 ID、姓名、战队、等级、胜率和英雄  
实际输出：显示 `P001`、昵称“阿离同学”、战队“长安星火”、等级、胜率和 3 名英雄。  
结果：通过

## TC05 按姓名查询玩家

功能：玩家查询  
输入：玩家昵称关键字  
预期输出：显示匹配玩家候选，选择后显示详情；如果完整昵称精确命中唯一玩家，则直接显示详情
实际输出：使用昵称关键字可以返回候选玩家，并在选择后显示其英雄和装备信息。
结果：通过

## TC06 战队概览

功能：战队查询  
输入：`T001`  
预期输出：显示成员、平均等级、总场次、胜率、顶尖玩家  
实际输出：输入 `T001` 可显示成员、平均等级、总对战数、胜率和顶尖玩家。  
结果：通过

## TC07 英雄详情

功能：英雄查询  
输入：英雄名称  
预期输出：显示英雄类型、属性、兼容装备、拥有者和推荐装备  
实际输出：输入英雄名称可显示类型、属性、兼容装备、拥有者和推荐装备。  
结果：通过

## TC08 装备统计

功能：装备排名  
输入：菜单选择装备统计  
预期输出：按装备综合实力排序，显示综合实力、估算使用、适配英雄和贝叶斯胜率
实际输出：装备列表按综合实力降序显示，包含类型、估算使用、适配英雄、贝叶斯胜率和综合实力。
结果：通过

## TC09 玩家对战历史

功能：对战历史  
输入：玩家 ID 和 N  
预期输出：显示最近 N 场对战  
实际输出：玩家历史按日期倒序显示，并输出英雄选用率。  
结果：通过

## TC10 排行榜

功能：排行榜  
输入：分别按胜率、等级、对战次数和综合实力显示前 5 名
预期输出：可按四种指标排序；综合实力使用贝叶斯胜率、等级归一化、对战量归一化和英雄多样性计算
实际输出：管理员菜单中四种排行榜均可显示；综合实力榜输出每名玩家的综合实力分。
结果：通过

## TC11 管理员添加玩家

功能：数据管理  
输入：新玩家 ID、姓名、等级  
预期输出：玩家加入数据集合  
实际输出：管理员可通过数据管理菜单添加玩家，重复 ID 会提示操作失败。  
结果：通过

## TC12 JSON 保存加载

功能：文件 I/O  
输入：保存数据后重新加载  
预期输出：数据数量和主要查询结果保持一致  
实际输出：程序显示“数据已保存到 data/game-data.json”，随后加载显示“数据已从 data/game-data.json 加载”。  
结果：通过

## 编译和烟测记录

编译命令：

```bash
javac -encoding UTF-8 -d out src/Main.java src/model/*.java src/service/*.java src/util/*.java
```

结果：通过，无编译错误。

烟测输入：

```text
4
3
2
1
P001
0
0
```

结果：通过，程序完成保存、加载、公开查询玩家并退出。

## TC13 连续登录多个玩家账号

功能：连续同角色登录稳定性  
输入：`P001/123456` 登录后登出，继续 `P002/123456` 登录后登出，继续 `P006/123456` 登录后登出  
预期输出：每次都进入玩家菜单，并显示对应玩家昵称  
实际输出：依次显示“欢迎 阿离同学”、“欢迎 峡谷小周”、“欢迎 法术核心”  
结果：通过  
发现的 bug：无

## TC14 连续登录多个管理员账号

功能：连续同角色登录稳定性  
输入：`admin/admin123` 登录后登出，继续 `coach/coach123` 登录后登出  
预期输出：每次都进入管理员菜单，并显示对应管理员名称  
实际输出：依次显示“欢迎 系统管理员”、“欢迎 战术教练”  
结果：通过  
发现的 bug：原数据集只有一个管理员，已补充 `coach/coach123` 用于覆盖该场景

## TC15 管理员和玩家混合连续登录

功能：跨角色登录稳定性  
输入：`admin/admin123` 登出，`P012/123456` 登出，`coach/coach123` 登出  
预期输出：管理员进入管理员菜单，玩家进入玩家菜单，再次管理员进入管理员菜单  
实际输出：菜单角色均正确切换，没有旧权限残留  
结果：通过  
发现的 bug：无

## TC16 失败登录不复用旧用户

功能：认证状态清理  
输入：成功登录并登出后，使用 `admin/wrong` 登录  
预期输出：显示登录失败，并停留主菜单  
实际输出：显示“登录失败，请检查用户名和密码。”，没有进入旧用户菜单  
结果：通过  
发现的 bug：无

## TC17 JSON加载后新增管理员可登录

功能：文件 I/O 后认证稳定性  
输入：先加载 `data/game-data.json`，再使用 `coach/coach123` 登录  
预期输出：新增管理员从 JSON 加载后仍可登录  
实际输出：显示“数据已从 data/game-data.json 加载”，随后“欢迎 战术教练”  
结果：通过  
发现的 bug：无

## TC18 主菜单非数字和越界输入

功能：菜单输入容错  
输入：主菜单依次输入 `abc`、`9`、`0`  
预期输出：非数字提示有效整数，越界数字提示合法范围，最后正常退出  
实际输出：显示“请输入有效整数。”、“请输入 0 到 4 之间的数字。”，随后退出  
结果：通过  
发现的 bug：无

## TC19 必填文本为空

功能：文本输入容错  
输入：登录时用户名先输入空行，再输入 `admin/admin123`  
预期输出：空用户名被拒绝，随后可以正常登录  
实际输出：显示“输入不能为空。”，随后管理员登录成功  
结果：通过  
发现的 bug：无

## TC20 新增对战记录日期格式错误

功能：日期输入容错  
输入：管理员新增对战记录，日期先输入 `2026-99-99`，再输入 `2026-06-04`  
预期输出：错误日期不崩溃，提示重输，正确日期后继续添加  
实际输出：显示“日期格式无效，请使用 yyyy-MM-dd，例如 2026-06-04。”，随后对战记录添加成功  
结果：通过  
发现的 bug：原实现直接使用 `LocalDate.parse`，错误日期可能导致异常；已改为 `InputHelper.readDate()` 循环校验

## TC21 外部JSON新增战队

功能：长期运营外部数据更新  
输入：在 `data/game-data.json` 的 `teams` 中添加 `T004` 云梦试训队，启动程序后查询 `T004`  
预期输出：无需修改 Java 代码即可查询到新战队  
实际输出：系统启动时自动加载 JSON，查询 `T004` 显示“云梦试训队”  
结果：通过  
发现的 bug：无

## TC22 外部JSON新增装备并参与推荐

功能：长期运营外部数据更新  
输入：在 `equipment` 中添加 `E021` 日渊，并把 `E021` 加入后羿 `H003` 的兼容装备列表，启动后查询 `H003`  
预期输出：后羿详情显示“日渊”，推荐装备包含“日渊”  
实际输出：兼容装备显示“日渊”，推荐装备显示“破军, 无尽战刃, 日渊”  
结果：通过  
发现的 bug：无

## TC23 启动自动加载JSON数据

功能：文件 I/O 和长期运营入口  
输入：直接启动程序  
预期输出：程序自动加载 `data/game-data.json`  
实际输出：显示“已自动加载数据文件 data/game-data.json”  
结果：通过  
发现的 bug：原实现需要手动选择“加载JSON数据”；已改为启动时自动加载，失败时回退内置初始数据

## TC24 外部JSON格式损坏时回退内置数据

功能：外部数据容错  
输入：临时将 `data/game-data.json` 改为非法 JSON 后启动程序  
预期输出：程序不崩溃，提示自动加载失败，并使用内置初始数据  
实际输出：显示“未能自动加载 data/game-data.json，使用内置初始数据。”，随后正常进入主菜单  
结果：通过  
发现的 bug：无

## TC25 数据集输出准确性逐项评测

功能：输出准确性综合验证  
输入：依次运行玩家 `P001`、战队 `T001`、战队 `T004`、英雄 `H003`、装备统计、玩家 `P001` 对战历史、战队 `T002` 对战历史和胜率排行榜  
预期输出：各项输出与 `data/game-data.json` 人工计算结果一致  
实际输出：全部一致；战队历史结果显示已从 `WIN/LOSE` 修复为“胜利/失败”  
结果：通过  
发现的 bug：战队历史显示语言不统一，已修复

## TC26 玩家ID大写和排行榜同位排序规则

功能：数据规范和排行榜排序  
输入：使用 `P001/123456` 登录；查询 `P001`；查看胜率排行榜前 5  
预期输出：玩家 ID 全部显示为 `P001` 形式；排行榜提示单局比赛没有平局，同位时按等级、胜率、对战次数和 ID 处理  
实际输出：`P001` 登录、查询和排行榜均正常；排行榜显示“同位排序：除当前排序条件外，依次按等级、胜率、对战次数降序，再按ID升序。单局比赛没有平局。”  
结果：通过  
发现的 bug：无

## TC27 公开玩家查询候选和超级账户保护

功能：公开玩家查询
输入：公开查询 -> 玩家查询 -> `P001`；公开查询 -> 玩家查询 -> `001` -> 选择 `1`；公开查询 -> 玩家查询 -> `coach`；公开查询 -> 玩家查询 -> `A001`
预期输出：`P001` 精确命中时直接显示玩家详情；`001` 部分关键字显示候选玩家选择界面；`coach` 和 `A001` 提示未找到玩家，不显示教练或管理员信息
实际输出：`P001` 直接进入详情；`001` 显示候选“阿离同学(P001)”并可选择；`coach` 和 `A001` 均显示“未找到玩家。”
结果：通过
发现的 bug：原实现只对昵称做模糊匹配，ID 和用户名片段会误报未找到；已改为玩家 ID、用户名和昵称均支持模糊候选，超级账户不进入公开检索。

## TC28 Swing GUI 编译和启动入口

功能：Java 可视化入口
输入：执行 `javac -encoding UTF-8 -d out src/Main.java src/GuiMain.java src/model/*.java src/service/*.java src/util/*.java src/gui/*.java`
预期输出：控制台版本和 Swing GUI 版本均可编译
实际输出：编译通过，无错误
结果：通过
发现的 bug：无

## TC29 Swing GUI 功能覆盖检查

功能：Swing GUI 可视化
输入：执行 `java -cp out GuiMain`，在 GUI 中依次查看玩家查询、战队概览、英雄详情、排行榜和编辑信息页签
预期输出：GUI 可以加载 `data/game-data.json`，并支持玩家查询、战队概览、英雄详情、排行榜和编辑信息
实际输出：Swing GUI 已实现六个页签；玩家、战队和英雄使用搜索框、候选列表和详情面板；排行榜使用表格并显示同位排序规则；对战模拟页签生成预览战报；编辑信息页签根据登录身份切换内容；窗口标题和页内主标题显示“王者荣耀信息管理系统”，不显示 AI 相关词句、提示语或 Swing 可视化提示文案
结果：通过
发现的 bug：无

## TC30 UML 文件检查

功能：UML 设计说明
输入：查看 `docs/class-diagram.puml`、`docs/class-diagram.md` 和 `IMPORTANT_GUI_AND_UML_NOTICE.md`
预期输出：存在电子 UML 文件和醒目的作业可视化说明
实际输出：PlantUML 与 Mermaid 两种类图文件均已创建，醒目说明文件明确 Swing GUI 是正式作业可视化、Node.js 可视化分支不是作业内容
结果：通过
发现的 bug：无

## TC31 GUI 加入后的连续登录退出回归

功能：登录状态稳定性
输入：连续执行 `admin/admin123`、`coach/coach123`、`P001/123456`、`P012/123456` 登录和登出，最后执行 `admin/wrong`
预期输出：管理员进入管理员菜单，玩家进入玩家菜单，错误密码停留主菜单
实际输出：连续登录和退出均正常，失败登录没有复用旧用户状态
结果：通过
发现的 bug：无

## TC32 连续添加和删除玩家信息

功能：管理员数据管理稳定性
输入：管理员连续添加 `P901`、`P902`，再连续删除 `P901`、`P902`，随后公开查询两个 ID
预期输出：添加成功，删除成功，删除后公开查询提示未找到玩家
实际输出：添加和删除均成功，公开查询显示“未找到玩家。”
结果：通过
发现的 bug：无

## TC33 GUI 与源程序兼容烟测

功能：Swing GUI 兼容性
输入：运行 `java -cp out test.GuiCompatibilitySmokeTest`
预期输出：GUI 六个页签存在；公开搜索不暴露超级账户；连续增删玩家后排行榜稳定；退出登录后编辑页回到未登录状态
实际输出：输出 `GUI compatibility smoke test passed.`
结果：通过
发现的 bug：无

## TC34 控制台保存后 GUI 加载

功能：JSON 持久化与 GUI 兼容性
输入：临时备份正式 JSON，控制台新增 `P930` 并保存，再用 `FileStorageService` 和 `VisualizationFrame` 加载，最后还原 JSON
预期输出：保存后的 JSON 能被 GUI 路径加载；测试后正式数据不残留临时玩家
实际输出：加载后玩家数为 16，`P930` 存在，GUI 窗口对象可创建；随后已还原数据文件
结果：通过
发现的 bug：无

## TC35 带 BOM JSON 加载

功能：外部数据兼容性
输入：构造临时带 UTF-8 BOM 的 `game-data.json` 副本并加载
预期输出：程序能剥离 BOM 并正常解析数据
实际输出：修复 `FileStorageService.load()` 后，带 BOM JSON 可正常加载
结果：通过
发现的 bug：原解析器不能处理文件开头的 UTF-8 BOM，已修复。

## TC36 Swing GUI 登录功能

功能：GUI 身份验证
输入：在 Swing GUI 中依次执行 `P001/123456` 登录、登出、`admin/admin123` 登录、登出、`admin/wrong` 登录
预期输出：玩家登录后显示“阿离同学”并可定位到自己的玩家详情；管理员登录后显示“系统管理员”；切换登录时如果密码错误，登录失败且回到未登录状态，不显示管理员或教练状态
实际输出：`GuiCompatibilitySmokeTest` 已覆盖该流程并输出 `GUI compatibility smoke test passed.`
结果：通过
发现的 bug：原 Swing GUI 只实现公开可视化，未接入 `AuthenticationService`；已补充登录、登出和“我的信息”入口。

## TC37 Swing GUI 编辑信息页签

功能：GUI 按身份编辑与登出回退
输入：打开 `java -cp out GuiMain`；未登录查看“编辑信息”；使用 `P001/123456` 登录后查看并修改自己的昵称或密码；登出；使用 `admin/admin123` 登录后查看管理员数据维护界面；再用错误密码切换登录
预期输出：未登录时“编辑信息”居中显示“登录以查看信息。”；玩家只能编辑自己的昵称和密码；管理员可选择管理员、玩家、英雄、装备、战队、对战记录并连续新建、保存或删除；任何登出或错误密码切换后，编辑页回到未登录状态
实际输出：`GuiCompatibilitySmokeTest` 已验证初始未登录、玩家登录、登出、管理员登录和错误密码后均切换到正确编辑卡片；手动检查确认按钮和数据类型表单可显示
结果：通过
发现的 bug：新增编辑页后需确保 `CardLayout` 状态随 `AuthenticationService.logout()` 同步切回未登录卡片；已增加状态刷新和烟测断言。

## TC38 管理员失败登录身份隐藏

功能：登录安全提示
输入：控制台或 GUI 中输入 `admin/wrong`、`coach/wrong`
预期输出：失败时只显示“登录失败，请检查用户名和密码。”，不提示“管理员账号登录失败”或“教练账号登录失败”；只有 `admin/admin123`、`coach/coach123` 成功登录后才显示管理员/教练身份
实际输出：控制台失败登录只显示通用失败文案；GUI 烟测验证错误密码后登录状态为“未登录”，且不包含“管理员”或“教练”
结果：通过
发现的 bug：无

## TC39 派生数据一致性多轮测试

功能：添加或删除玩家、英雄、装备后的动态信息同步
输入：运行 `java -cp out test.DerivedDataConsistencyTest`
预期输出：新增/删除玩家时，战队成员、平均等级和顶尖玩家实时变化；战队历史总对战数和胜率保持不变；新增/删除英雄时，英雄拥有玩家和玩家英雄列表同步；新增/删除装备时，推荐装备、兼容装备列表和装备统计同步
实际输出：输出 `Derived data consistency test passed.`；详见 `docs/derived-data-consistency-report.md`
结果：通过
发现的 bug：测试初稿曾把平均等级预期写成整数除法，已修正为浮点计算；业务代码未发现不一致。

## TC40 综合实力公式精确测试

功能：玩家排行榜和装备排名的自定义综合实力公式
输入：运行 `java -cp out test.ComprehensiveRankingTest`
预期输出：玩家综合实力公式为 `100*(0.40*BayesianWinRate+0.25*LevelScore+0.25*MatchVolumeScore+0.10*HeroDiversityScore)`；装备综合实力公式为 `100*(0.35*BayesianWinRate+0.25*PopularityScore+0.25*RatingScore+0.15*HeroCoverageScore)`；合成数据中的 `P100` 和 `E100` 应排名第一
实际输出：输出 `Comprehensive ranking test passed.`
结果：通过
发现的 bug：无

## TC41 中文姓名重叠候选检索

功能：公开玩家中文模糊查询候选
输入：在内存数据中新增中文姓名玩家 `阿离新星(P881)`，与现有玩家 `阿离同学(P001)` 共享中文片段；运行 `java -cp out test.ChineseSearchCandidateTest`，检索中文词条 `阿离`
预期输出：候选结果同时包含 `阿离同学(P001)` 和 `阿离新星(P881)`；精确检索 `阿离新星` 只命中新增玩家
实际输出：输出 `Chinese search candidate test passed.`，候选列表同时打印两个中文相关玩家
结果：通过
发现的 bug：使用 PowerShell here-string 直接向控制台脚本传中文时，终端输入编码可能把中文显示为 `????`，因此该自动测试使用 Java 字符串验证，避免被终端编码误导。

## TC42 Swing GUI 管理员新建与删除保护

功能：GUI 管理员数据维护防误操作
输入：管理员登录 Swing GUI，进入“编辑信息”；选择“玩家”后点击“新建”；观察玩家ID和用户名；只填写部分必填项后保存；再补齐用户名、密码、昵称，战队ID 分别尝试 `T001`、`001`、`01`、`1`；尝试填写未创建的 `T999` 和 `H999`；分别选择“英雄”“装备”“战队”“对战记录”后点击“新建”，检查 ID 是否自动生成且不可编辑，并提交空白或非法引用表单；战队可只填写名称创建空战队；删除玩家、英雄、装备、战队或对战记录时点击删除按钮
预期输出：新玩家玩家ID自动显示当前最大玩家编号下一位，如 `P016`，且不可编辑；用户名预填为 `P016` 但可编辑；用户名、密码、昵称缺失时弹窗按固定顺序提示缺失项；等级空白默认 `1`，胜场和负场空白默认 `0`，胜率为 `0.00%`；战队ID 的四种写法均归档为 `T001`，英雄ID 同理归档为 `Hxxx`；若归档后的战队或英雄ID不存在，弹窗提示 `战队ID不存在` 或 `英雄ID不存在`，且不保存玩家；新英雄、新装备、新战队、新对战记录分别自动显示 `Hxxx`、`Exxx`、`Txxx`、`Mxxx` 且 ID 不可编辑；英雄名称、类型、攻击、防御、技能和装备ID全为必填，且装备ID必须存在；战队名称必填，成员ID可为空，但填写时必须存在；对战记录日期、队伍A、队伍B、胜者战队和英雄选择全为必填，队伍、玩家和英雄引用必须存在，胜者必须是队伍A或队伍B；删除前显示确认弹窗；若保存时目标 ID 已存在，则先弹窗询问是否覆盖
实际输出：编译和 `GuiCompatibilitySmokeTest`、`DerivedDataConsistencyTest` 均通过；`GuiCompatibilitySmokeTest` 已验证非法 `T999/H999` 被拒绝且 `P889` 不残留，也验证非法 `E999`、`P999`、`M889` 对战引用被拒绝，空战队 `T889` 可先创建再删除；手动检查 Swing 表单逻辑确认 ID 预设、必填提示、ID 归一化、覆盖确认和删除确认均已接入
结果：通过
发现的 bug：原 GUI 允许复用已有 ID 直接覆盖玩家或装备信息，存在数据损失风险；原数据管理层也允许保存未创建的战队、英雄、装备或对战引用，形成悬空档案。已增加覆盖确认、删除确认、自动 ID 和引用存在性校验。

## TC43 对战模拟预览

功能：对战模拟
输入：运行 `java -cp out test.CombatSimulatorTest`；在控制台公开查询中选择“对战模拟”，输入 `T001` 和 `T002`；在 Swing GUI “对战模拟”页签选择两支战队并点击“开始模拟”
预期输出：系统根据当前战队成员、玩家等级、拥有英雄、英雄属性和兼容装备自动选择阵容，生成 5v5 回合战报；少于 5 名可参赛成员时拒绝模拟；超过 5 人时随机抽取 5 人上场；战报包含地图节奏、团队士气、装备触发、暴击、闪避和最终胜者；模拟只预览，不写入 `MatchRecord`，不改变玩家胜负场、战队历史胜率或排行榜
实际输出：`CombatSimulatorTest` 使用固定随机种子验证两次模拟报告一致，`T001` vs `T002` 生成 5 回合并有唯一胜者；`T001` vs `T001` 被拒绝；空战队 `T004` 和临时构造的 4 人战队被拒绝；临时构造的 6 人战队只随机选 5 人上场；模拟前后的对战记录数量、`P001` 胜负场和胜率榜第一名保持不变。Swing GUI 烟测确认新增“对战模拟”页签。
结果：通过
发现的 bug：无
