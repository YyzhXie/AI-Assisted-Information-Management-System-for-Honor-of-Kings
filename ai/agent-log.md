# 智能体日志

## 架构师智能体

主要贡献：根据课程材料规划纯 Java 控制台架构，确定模型层、服务层和菜单层，保留必需类和接口。

人工决定：接受 `Person` 抽象父类、`Player` 和 `Admin` 继承、服务类分层；拒绝引入 Maven、Gradle 和 Node.js 依赖。

相关提交：

- e4b0f42 添加核心模型类和枚举
- 64ea21c 添加服务接口和数据管理骨架
- 8b7502c 完善类关系和接口设计说明

## 实现智能体

主要贡献：实现模型类、服务类、数据初始化、JSON 保存加载和控制台菜单。

人工决定：将 JSON 解析限制为项目固定格式，避免引入难解释的第三方库。

相关提交：

- b9b53d0 添加课程要求初始数据集
- 314958f 实现查询排行和推荐报表
- ee0cc3e 实现JSON文件保存和加载
- b2347d3 实现控制台菜单和权限流程

## 测试 / 评审智能体

主要贡献：检查功能覆盖、编译错误、菜单流程和测试文档。

人工决定：采用手动测试文档作为主要测试证据，并通过 `javac` 和控制台输入做最终验证。

相关提交：

- 80a39df 生成并验证初始JSON数据文件
- b2e5ddd 记录编译和菜单烟测结果
- ddfb37f 添加功能覆盖评审清单

补充评测：

- 2026-06-04 对连续登录稳定性进行专项评测，覆盖多个玩家、多个管理员、管理员/玩家混合切换和错误密码场景。
- 人工决定：原数据集只有一个管理员，不足以测试“多个同类管理员账号”，因此补充 `coach/coach123` 并同步到初始化数据和 JSON 数据。
- 2026-06-04 对交互输入鲁棒性和外部 JSON 运营更新进行专项评测，覆盖非数字、越界、空输入、错误日期、自动加载 JSON、新增战队和新增装备推荐。
- 人工决定：接受启动自动加载 JSON 和日期循环校验的改进，让纯 Java 项目更适合长期维护数据。
- 2026-06-04 对数据集输出准确性逐项评测，覆盖玩家详情、战队概览、空战队、英雄推荐、装备统计、对战历史和排行榜。
- 人工决定：发现战队历史结果显示为英文 `WIN/LOSE`，数据正确但显示不统一，因此修复为中文“胜利/失败”。
- 2026-06-04 根据用户决策更新比赛和排行榜规则：单局比赛不存在平局；排行榜同位时按等级、胜率、对战次数和 ID 稳定排序。
- 人工决定：将所有玩家 ID 从旧的小写前缀形式统一改为 `P001` 这类大写前缀形式，并同步初始化数据、JSON 数据和测试文档。
- 2026-06-04 修复公开玩家查询的部分关键字问题：玩家 ID、用户名和昵称均支持模糊候选，精确命中唯一玩家时直接进入详情或历史。
- 人工决定：管理员 `admin` 和教练 `coach` 属于超级账户，不允许通过公开玩家查询被枚举，因此超级账户关键字统一提示“未找到玩家”。
- 2026-06-05 在 `codex/node-visualization-dashboard` 分支新增 Node.js 可视化版本，复用 `data/game-data.json` 展示总览图表、玩家筛选、战队详情、英雄推荐、装备评分和对战时间线。
- 人工决定：可视化能力对课程展示同样重要；Node.js 版本作为独立分支和独立子项目实现，不替代 Java 控制台主线，并且不向浏览器暴露管理员、教练和密码字段。
- 2026-06-05 根据重新确认的作业要求，在 `main` 分支新增 Java Swing GUI 可视化版本，覆盖玩家查询、战队概览、英雄详情和排行榜。
- 人工决定：正式作业可视化使用 Java Swing；此前 Node.js 可视化分支仅作为额外实验，不作为课程作业提交内容。同时补充 PlantUML 和 Mermaid 电子 UML 文件。
- 2026-06-05 对加入 Swing GUI 后的运行稳定性进行回归评测，覆盖连续登录退出、连续添加/删除玩家、超级账户公开搜索边界、控制台保存后 GUI 加载和 GUI 页签兼容烟测。
- 人工决定：不新增批量删除或批量添加功能，仅按现有数据管理能力测试连续删除和连续添加。评测中发现带 BOM JSON 加载失败的问题，已在 `FileStorageService` 中修复。
- 2026-06-05 修复 Swing GUI 忽略登录功能的问题，将 `VisualizationFrame` 接入 `AuthenticationService`，新增登录、登出和“我的信息”入口。
- 人工决定：GUI 保留公开可视化能力，但应像控制台一样支持用户身份验证；玩家登录后自动定位个人详情，管理员和教练登录后显示身份状态。
- 2026-06-05 在 Swing GUI 中新增“编辑信息”页签，按未登录、玩家和管理员三种状态切换界面。
- 人工决定：登出后编辑页签必须回到未登录提示状态；玩家只能编辑自己的基础信息，管理员可维护全部主要数据。
- 2026-06-05 加强登录失败提示安全性，管理员或教练账号失败登录时只显示通用失败文案。
- 人工决定：只有管理员或教练账号成功登录之后，系统才显示管理员身份；失败登录不得提示“管理员账号登录失败”。
- 2026-06-05 对添加/删除玩家、英雄、装备后的派生数据一致性进行多轮测试，覆盖战队统计、英雄拥有玩家、推荐装备和装备统计。
- 人工决定：战队总对战数和胜率由历史对战记录决定，仅添加或删除玩家时不应改变这两项；成员、平均等级和顶尖玩家应随成员变化实时更新。
- 2026-06-05 根据对 Gemini 排名方案的比较，新增玩家和装备“综合实力”自定义排名指标。
- 人工决定：不直接采用简单加权相加，而使用归一化和贝叶斯胜率修正；装备胜率贡献明确标注为估算值，不伪装成真实购买数据。
